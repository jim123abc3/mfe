name: Test

on:
  push:
    branches: [main]
      paths: ['packages/container/**']

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hello